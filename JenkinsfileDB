pipeline {
    agent any
    environment {
        DOCKER_COMPOSE_FILE = 'docker-compose.yml'
    }
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/Soufyan-Lekhouaja/ecom_app', branch: 'main'
            }
        }
        stage('Start MySQL Service') {
            steps {
                // Launch only the mysql8 service
                bat "docker-compose -f ${env.DOCKER_COMPOSE_FILE} up -d mysql8"
            }
        }
        stage('Wait for MySQL Healthy') {
            steps {
                script {
                    bat '''
:loop
SET STATE=\"$(docker inspect --format '{{.State.Health.Status}}' mysql8)\"
IF "%STATE%"=="healthy" GOTO done
TIMEOUT /T 5 > NUL
GOTO loop
:done
'''
                }
            }
        }
    }
    post {
        failure {
            echo 'MySQL deployment failed.'
        }
    }
}
